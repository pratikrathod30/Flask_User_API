Flask User API

This is a simple REST API built using Flask as part of Task-4 of the Python Developer Internship.

The API performs basic CRUD operations (Create, Read, Update, Delete) on users stored in an in-memory list.

Features

Add new user

Get all users

Get user by ID

Update user

Delete user

Requirements

Python

Flask

Postman
